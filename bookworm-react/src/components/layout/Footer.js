import React from "react";
import Grid from "@material-ui/core/Grid";

export default props => (
  <Grid container justify="center">
    <Grid item align-items-xs-center>
      center
    </Grid>
  </Grid>
);
